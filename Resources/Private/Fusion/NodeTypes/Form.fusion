prototype(DRKTettnang.Homepage:Form) < prototype(Neos.Neos:Plugin) {
      package = 'DRKTettnang.Homepage'
      controller = 'Form'
      action = 'step1'

      form = ${q(node).property('form')}
}
